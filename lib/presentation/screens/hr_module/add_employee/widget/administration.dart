import 'package:flutter/material.dart';

class AdministartionTab extends StatelessWidget {
  const AdministartionTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.redAccent,
    );
  }
}
