import 'package:flutter/material.dart';

class AddEmplyeeScreen extends StatelessWidget {
  const AddEmplyeeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: Center(
        child: Text('Banking'),
      ),
    );
  }
}
